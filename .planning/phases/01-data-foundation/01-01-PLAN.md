---
phase: 1
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - apps/quantedge_core/Cargo.toml
  - apps/quantedge_core/crates/data/Cargo.toml
  - apps/quantedge_core/crates/data/src/lib.rs
  - apps/quantedge_core/crates/core/Cargo.toml
  - apps/quantedge_core/crates/core/src/lib.rs
  - apps/quantedge_core/crates/greeks/Cargo.toml
  - apps/quantedge_core/crates/greeks/src/lib.rs
  - apps/quantedge_core/crates/metrics/Cargo.toml
  - apps/quantedge_core/crates/metrics/src/lib.rs
  - apps/quantedge_core/crates/portfolio/Cargo.toml
  - apps/quantedge_core/crates/portfolio/src/lib.rs
  - apps/quantedge_core/crates/optimizer/Cargo.toml
  - apps/quantedge_core/crates/optimizer/src/lib.rs
  - apps/quantedge_core/crates/nif/Cargo.toml
  - apps/quantedge_core/crates/nif/src/lib.rs
autonomous: true
requirements:
  - DATA-01
---

# Plan 01-01: Rust Workspace Setup and Crate Structure

<objective>
Set up the Rust workspace with all 7 crates (data, core, greeks, metrics, portfolio, optimizer, nif) as empty shells. Each crate compiles and the workspace builds cleanly. This establishes the project structure that all subsequent plans build upon.
</objective>

<tasks>

<task id="1">
<type>execute</type>
<files>
  - Cargo.toml
</files>
<action>
Create the root `Cargo.toml` as a Rust workspace definition:

```toml
[workspace]
resolver = "2"
members = [
  "apps/quantedge_core/crates/data",
  "apps/quantedge_core/crates/core",
  "apps/quantedge_core/crates/greeks",
  "apps/quantedge_core/crates/metrics",
  "apps/quantedge_core/crates/portfolio",
  "apps/quantedge_core/crates/optimizer",
  "apps/quantedge_core/crates/nif",
]

[workspace.dependencies]
polars = { version = "0.53", features = ["lazy", "parquet", "streaming", "csv"] }
chrono = { version = "0.4", features = ["serde"] }
toml = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ndarray = "0.16"
statrs = "0.17"
rayon = "1.10"
rustler = "0.37"
criterion = "0.5"
```
</action>
<read_first>
  - README.md (project structure reference)
  - Docs/PRD.md (lines 1-20, crate names)
</read_first>
<acceptance_criteria>
  - Cargo.toml exists at project root
  - Cargo.toml contains `[workspace]` section
  - Cargo.toml contains `resolver = "2"`
  - `members` array lists all 7 crate paths
  - `[workspace.dependencies]` includes polars, chrono, toml, serde
</acceptance_criteria>
</task>

<task id="2">
<type>execute</type>
<files>
  - apps/quantedge_core/crates/data/Cargo.toml
  - apps/quantedge_core/crates/data/src/lib.rs
</files>
<action>
Create the `quantedge-data` crate:

`Cargo.toml`:
```toml
[package]
name = "quantedge-data"
version = "0.1.0"
edition = "2021"

[dependencies]
polars = { workspace = true }
chrono = { workspace = true }
toml = { workspace = true }
serde = { workspace = true }

[dev-dependencies]
criterion = { workspace = true }
```

`src/lib.rs`:
```rust
//! QuantEdge Data Layer
//!
//! Handles Parquet I/O, expiry calendar, IV surface interpolation,
//! and lot size configuration.

pub mod bar;
pub mod expiry;
pub mod iv_surface;
pub mod lot_sizes;
```

Create empty module files:
- `src/bar.rs` with `//! Bar data types and Parquet reader.`
- `src/expiry.rs` with `//! Expiry calendar resolution.`
- `src/iv_surface.rs` with `//! IV surface interpolation.`
- `src/lot_sizes.rs` with `//! Lot size configuration.`
</action>
<read_first>
  - Docs/PRD.md (data schema section)
</read_first>
<acceptance_criteria>
  - apps/quantedge_core/crates/data/Cargo.toml exists and has `name = "quantedge-data"`
  - apps/quantedge_core/crates/data/src/lib.rs exists with 4 pub mod declarations
  - apps/quantedge_core/crates/data/src/bar.rs exists
  - apps/quantedge_core/crates/data/src/expiry.rs exists
  - apps/quantedge_core/crates/data/src/iv_surface.rs exists
  - apps/quantedge_core/crates/data/src/lot_sizes.rs exists
</acceptance_criteria>
</task>

<task id="3">
<type>execute</type>
<files>
  - apps/quantedge_core/crates/core/Cargo.toml
  - apps/quantedge_core/crates/core/src/lib.rs
  - apps/quantedge_core/crates/greeks/Cargo.toml
  - apps/quantedge_core/crates/greeks/src/lib.rs
  - apps/quantedge_core/crates/metrics/Cargo.toml
  - apps/quantedge_core/crates/metrics/src/lib.rs
  - apps/quantedge_core/crates/portfolio/Cargo.toml
  - apps/quantedge_core/crates/portfolio/src/lib.rs
  - apps/quantedge_core/crates/optimizer/Cargo.toml
  - apps/quantedge_core/crates/optimizer/src/lib.rs
  - apps/quantedge_core/crates/nif/Cargo.toml
  - apps/quantedge_core/crates/nif/src/lib.rs
</files>
<action>
Create the remaining 6 crate shells. Each has a minimal Cargo.toml with workspace dependencies and a src/lib.rs stub:

**quantedge-core** (Cargo.toml name: `quantedge-core`):
- Dependencies: `quantedge-data = { path = "../data" }`, chrono, serde
- lib.rs: `//! Simulation engine: Leg, Position, SL state machine, runner.`

**quantedge-greeks** (name: `quantedge-greeks`):
- Dependencies: `quantedge-data = { path = "../data" }`, ndarray, statrs, chrono
- lib.rs: `//! Greeks computation: Black-Scholes, delta/gamma/theta/vega.`

**quantedge-metrics** (name: `quantedge-metrics`):
- Dependencies: `quantedge-data = { path = "../data" }`, `quantedge-greeks = { path = "../greeks" }`, statrs, ndarray, chrono
- lib.rs: `//! All 75+ metrics, walk-forward, Monte Carlo.`

**quantedge-portfolio** (name: `quantedge-portfolio`):
- Dependencies: `quantedge-core = { path = "../core" }`, `quantedge-metrics = { path = "../metrics" }`, ndarray
- lib.rs: `//! Portfolio engine: multi-strategy, margin model, correlation.`

**quantedge-optimizer** (name: `quantedge-optimizer`):
- Dependencies: `quantedge-core = { path = "../core" }`, `quantedge-metrics = { path = "../metrics" }`, rayon
- lib.rs: `//! Parameter optimizer: grid sweep, Rayon parallelism.`

**quantedge-nif** (name: `quantedge-nif`):
- Dependencies: `quantedge-core = { path = "../core" }`, `quantedge-metrics = { path = "../metrics" }`, `quantedge-portfolio = { path = "../portfolio" }`, `quantedge-optimizer = { path = "../optimizer" }`, rustler, serde_json
- lib.rs: `//! Rustler NIF bridge for Elixir.`
- Also add `[lib]` section: `crate-type = ["cdylib"]`
</action>
<read_first>
  - Docs/PRD.md (crate structure overview)
</read_first>
<acceptance_criteria>
  - All 6 crate directories exist under apps/quantedge_core/crates/
  - Each has Cargo.toml with correct package name
  - Each has src/lib.rs with doc comment
  - quantedge-nif Cargo.toml has `crate-type = ["cdylib"]`
</acceptance_criteria>
</task>

<task id="4">
<type>execute</type>
<files>
  - .gitignore
</files>
<action>
Update .gitignore to include Rust build artifacts and data directories:

Add these entries (preserve existing content):
```
# Rust
target/
Cargo.lock

# Data (large files)
data/raw/
data/parquet/

# Python
__pycache__/
*.pyc
.venv/
```
</action>
<read_first>
  - .gitignore (current content)
</read_first>
<acceptance_criteria>
  - .gitignore contains `target/`
  - .gitignore contains `data/raw/`
  - .gitignore contains `data/parquet/`
</acceptance_criteria>
</task>

<task id="5">
<type>verify</type>
<files>
  - Cargo.toml
</files>
<action>
Run `cargo check` from the project root to verify the entire workspace compiles:

```bash
cargo check --workspace
```

All 7 crates should compile with 0 errors. Warnings are acceptable at this stage.
</action>
<read_first>
  - Cargo.toml
</read_first>
<acceptance_criteria>
  - `cargo check --workspace` exits with code 0
  - No compilation errors (warnings acceptable)
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Run `cargo check --workspace` — must exit 0
2. Verify all 7 crate directories exist: `ls apps/quantedge_core/crates/`
3. Verify workspace members: `grep -c "crates/" Cargo.toml` shows 7
</verification>

<success_criteria>
- Rust workspace compiles cleanly
- All 7 crate shells exist with correct names and dependencies
- workspace.dependencies defined for shared crate versions
- .gitignore covers Rust and data artifacts
</success_criteria>

<must_haves>
- [ ] Workspace Cargo.toml at project root with all 7 members
- [ ] Each crate has Cargo.toml + src/lib.rs
- [ ] `cargo check --workspace` passes
</must_haves>
